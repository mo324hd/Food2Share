// This file previously contained a duplicate SceneDelegate.
// It is intentionally excluded from compilation to avoid conflicts.

#if false
import UIKit

class AppSceneDelegate: UIResponder, UIWindowSceneDelegate {
    var window: UIWindow?
}
#endif
